# Test infrastructure consolidation

> **Status:** ✅ done — steps 1–3 landed (commits `73bf8184 remove unused shape definitions`, `ea28c649 [repl] remove unused code`, `8b1b19cf [css-test-utils] remove package and related files`); the client-id quadstore move + S3-seeding drop (old steps 4–6) were dropped by decision.
>
> Extracted from **Phase 6** of [`refactor-data-model.md`](refactor-data-model.md) (the Phase 5 rework now points here). Independent from Phases 1–5 of that plan and can land any time (before, after, or parallel to them). It touches **no data-model code** — only fixtures, test servers, and where tests live. Its goals: remove the in-process CSS server + `@janeirodigital/css-test-utils` entirely — the whole `discovery` test suite moves to root `test/` as mock-free integration tests against the docker stack, the rest of the server-touching tests become mocks or are deleted, and `packages/css-storage-fixture/test/registry.trig` stays the single fixture source. **The file-backed client id stays as-is** (decision after step 3): `test/data/test-client/public/id$.jsonld` → garage S3 via `test/setup.ts` → served by the hybrid accessor through the `ma:format` triple in the trig — so the planned quadstore move (old steps 4–6) is dropped, and `test/data`, `dev/data`, and the docker `data` service all remain untouched.

Doing it first **narrows Phase 5's scope** (only the mock realm `data.json` needs converting — done in the Phase 5 rework; `registry.trig` stays Turtle regardless — the quadstore-backed CSS content-negotiates JSON-LD on request) and **makes the per-phase gate of Phases 1–4 server-free** (most relevant to Phase 2, which rewrites the `Application`/`DataOwner` APIs that `application.test.ts`'s `describe.skip` block once exercised against a live CSS).

## Current topology (two fixture realms)

| Realm | Location | Server? | Consumers |
|-------|----------|---------|-----------|
| **Mock** | `packages/test-utils/src/data.json` + `fetch-mock.ts` | none (in-memory fetch mock) | data-model, authorization-agent, components unit tests — **stays** (JSON-LD since the Phase 5 rework) |
| **CSS server** | `packages/css-storage-fixture/test/` (pods `data/`, `solid/`, `luka/`, `vaporcg/`, `corvax/`, `galathor/`, `vortiga/`, `zenara/` + `.internal/` accounts) | in-process CSS on `:3711` via `SolidTestUtils` (`packages/css-test-utils`) | package tests: `application/test/application.test.ts`, `utils/test/discovery.test.ts` (repl's consumer was dropped in step 2) |
| **CSS server** | `packages/css-storage-fixture/dev/` (`dev/data`, `dev/pod`) | docker `data`/`auth` services + `registry` CSS (oxigraph) | root `test/*.ts` (docker stack): `setup.ts` seeds `registry.trig` → oxigraph, `kv.json` → Postgres, client id → garage S3 |

Key facts:

- `registry.trig` (loaded into oxigraph by `test/setup.ts`) **already serves 210 `https://data/...` resources** — data registries (`acme-rnd`, `acme-hr`, `alice-work`, …), instances, and even `test-client/public/access-needs` plus the shapetrees **trees/descriptions** (`Project`/`Task`/`File`/`Image`, `desc-en/es/pl` — identical content to the file copies). The only file-backed resource left under `https://data/` is the **client id document** (`test/data/test-client/public/id$.jsonld`, currently uploaded to garage S3). The `.shex` **shape documents are deleted, not moved** — nothing fetches them (`ShapeTreeData.shape` is just an IRI string; no ShEx parser or `@shapetrees/*` dependency anywhere in the repo) and the quadstore can't hold them anyway (non-RDF; the hybrid accessor routes non-quads to S3, so they're unserved today).
- The in-process CSS server lives in `packages/css-test-utils` (not `packages/test-utils`); `test-utils` has no server, only the mock fetch + Postgres client.
- `test/data` and `dev/data` **differ** (real drift — e.g. `bob`/`www` only in `dev`, differing ttl contents; the `.shex` shapes are deleted outright, so their drift is irrelevant).
- `test/solid/trees/` (Widget/Gadget) + the `solid/`/`luka/`/`vaporcg/`/`corvax/`/`galathor/`/`vortiga/`/`zenara/` pods (**7 total**) are a separate realm (`localhost:3711`, `solidshapes.example`) used by the package-CSS tests; all 7 pods are registered in `.internal` at `localhost:3711` and referenced nowhere else (not in `registry.trig`, root `test/`, or `.dagger/`); it's deleted with `css-test-utils` in step 3.

## Steps

1. **Remove the `.shex` shape documents outright** — delete `test/data/shapetrees/shapes/` + `dev/data/shapetrees/shapes/` and the dangling `meta:https://data/shapetrees/shapes/*` graphs in the trig (`Project`/`Task`/`Image`/`File`). The shapes are non-RDF, so they can't live in the quadstore (the hybrid accessor routes non-quads to S3, where nothing uploads them), and no code fetches them — `ShapeTreeData.shape` is just an IRI string, with no ShEx parser or `@shapetrees/*` dependency anywhere in the repo. Same for the file-only `Event`/`Role`/`cg-en` trees: unreferenced, drop them rather than move them. Also drop the two dead shape snippets in the mock `data.json` (`https://solidshapes.example/shapes/Project|Task`) and point the `fetch-mock.test.ts` tests using them at any other existing key (they only use them as generic snippet keys).
2. **Clean up `repl`** — delete the css-test-utils-dependent entrypoints: `cli.ts`, `repl.ts`, and `test/cli.test.ts` (+ its `test/services/` MockConsole/MockTerminal helpers) — `buildSession` in `cli.ts`/`repl.ts` boots the in-process CSS via `new SolidTestUtils(account)`, and `cli.test.ts` only tests `mainPrompt` from the deleted `cli.ts`. Drop the `repl`/`cli` npm scripts and the `@janeirodigital/css-test-utils` devDependency. **Keep `cmd.ts` + `add-user.ts`** — they don't depend on css-test-utils (interop packages only) and `cmd.ts` already imports `addUserCommand` from `./add-user.js`.
3. **Remove `@janeirodigital/css-test-utils` and the localhost:3711 realm** — remaining consumers and per-file changes:
   - `application/test/application.test.ts` (imports `SolidTestUtils, accounts, inspector, shapeTree`) — all usage is confined to the `describe.skip('describe discovery')` block (never runs) plus the `beforeAll`/`afterAll` server boot. Drop the import, the server hooks, and the skipped block — its APIs are rewritten in Phase 2 anyway; the two mock-based describes (`statelessFetch`) stay as the package's server-free gate.
   - `utils/test/discovery.test.ts` (imports `SolidTestUtils, accounts, host`) — **the whole file ports to root `test/discovery.test.ts`**, mock-free, against the docker stack with `registry.trig` data (the `statelessFetch` mocks die with it; the fns stay in `utils/src/` — used by `application`/`authorization-agent`). Per-test live targets:
     - `discoverAuthorizationAgent` → WebID `https://id/alice` (quadstore graph has `interop:hasAuthorizationAgent <https://auth/.sai/agents/aHR0cHM6Ly9pZC9hbGljZQ>`) — needs an `RdfFetch` adapter over a real fetch (`parseTurtle`, since the fn calls `.dataset()`).
     - `discoverAgentRegistration` → HEAD the agent IdP doc `https://auth/.sai/agents/aHR0cHM6Ly9pZC9hbGljZQ` expecting the `interop#registeredAgent` link (`https://registry/alice/agent/cvmsa4/` with alice+testClient — same values as `agent-discovery.test.ts`); likely needs `buildOidcSession` + `authFetch` (the link is session-relative).
     - `discoverDescriptionResource` → CSS wires `AuxiliaryLinkMetadataWriter` (`rel="describedby"`, via the default `link-rel-metadata.json` writer + the `MetadataStrategy` from `css:config/util/identifiers/suffix.json`) on all services, so **every `ldp:Resource` response carries the link** — test against the publicly-readable client id `https://data/test-client/public/id` (public read via the `wegvwe` grant) expecting `https://data/test-client/public/id.meta`; the agent IdP doc (non-LDP) covers the undefined case and a bad IRI the throw case.
     - `discoverStorageDescription` → **no authn needed**; one test against a *publicly readable* data resource (storage roots themselves are NOT readable: the SAI engine grants no modes to `TargetType.Registry`, and registry pods are ACR-locked): `https://data/test-client/public/id` → `https://data/test-client/.well-known/solid` (public via the `wegvwe` grant, `grantee acp:PublicClient`; `https://data/shapetrees/trees/Project` works too via `vled83`); the sai stack wires the advertiser via `sai:config/http/handler/default.json`. The `authFetch` calls need `.bind(session)` (passing `session.authFetch` unbounded breaks `SessionCore.authFetch`'s `this.isActive`); the "request fails" tests use `https://registry/nonexistent-path/` (the agent IdP handler returns 200 for any `/sai/agents/*` path).
     - `discoverAuthorizationRedirectEndpoint` + `discoverWebPushService` → fetch `https://data/test-client/public/id` (JSON-LD) — **blocked on extending the client id document**: it currently lacks `interop:hasAuthorizationRedirectEndpoint` / `interop:pushService` / `notify:vapidPublicKey` (only access-needs + callback endpoint); add them to `test/data/test-client/public/id$.jsonld` (done; stays S3-served — the planned trig mirror is dropped). Values confirmed (the agent IdP doc advertises `https://ui.auth/authorize` for `hasAuthorizationRedirectEndpoint`).
   - Delete the package; drop the devDependency from `application`, `utils` (repl's was dropped in step 2) — **edit `package.json` only; `package-lock.json` is regenerated by the user**; drop the `css:test` script in `css-storage-fixture` (it launches this realm).
   - Delete the realm data: `test/solid/`, `test/luka/`, `test/vaporcg/`, `test/corvax/`, `test/galathor/`, `test/vortiga/`, `test/zenara/`, `test/.internal/` — **all 7 pods + account/IdP state**, keyed to `localhost:3711` (the docker auth stack keeps its own state in Postgres + env JWK).
4–6. **Removed** — the client id stays S3-seeded (see the intro decision): no trig content graph, no `ma:format` removal, no `setup.ts` garage-drop, and no deletion of `test/data/` (still holds the client id + the shapetrees `trees/*$.ttl` copies), `dev/data`, or the docker `data` service.

## Stays as-is (boundary)

- **Mock realm** — `data.json` + `fetch-mock.ts` stay for package unit tests (JSON-LD since the Phase 5 rework).
- **`registry.trig` stays Turtle** — the quadstore seed format is independent of the client wire format; the CSS serves JSON-LD via content negotiation after Phase 5.
- **kv.json → Postgres** seeding in `setup.ts` stays (temporal workflow state).
- **Client id S3 seeding in `setup.ts` stays** (`garage.putAnyObject`/`deleteObject` around `test/data/test-client/public/id$.jsonld`) — the client id document remains the one file-backed resource under `https://data/`, served via the hybrid accessor (`ma:format` triple in the trig).
- Widget/Gadget shape trees stay in the CSS realm while the package-CSS tests still use them; their `.shex` documents aren't fetched (same as the data-realm shapes) and go away with the realm.

## Verify

- Package tests (`application`, `utils`) run **server-free** (mock fetch only); `repl` keeps only `cmd.ts`/`add-user.ts` and no package tests; `utils` has no discovery tests left (ported to root).
- Root integration tests (`test/*.ts`) pass against the docker stack — including `test/discovery.test.ts` (storage description on `https://data/test-client/public/id`, agent/WebID discovery, client-id fns against the extended doc) with the client id at `https://data/test-client/public/id` served from garage S3 as today.
- No references to `@janeirodigital/css-test-utils` remain (tests or repl source); `packages/css-storage-fixture/test/data/` and `dev/data` stay as-is.
- `https://data/shapetrees/shapes/*` 404s are expected (no consumer); the shape **trees** (`https://data/shapetrees/trees/*`) still resolve from the quadstore.
