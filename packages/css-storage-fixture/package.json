{
  "name": "css-storage-fixture",
  "private": true,
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "community-solid-server -c config.json -l warn",
    "start:sai": "community-solid-server -c sai.json -f ./sub -p 3100",
    "dev": "pnpm run clean && pnpm run start",
    "clean": "pnpm clean:empty-directories && pnpm clean:notification-channels",
    "clean:empty-directories": "find . -type d -empty -delete",
    "clean:notification-channels": "rm -rf .internal/notifications/*"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@solid/community-server": "^7.1.0",
    "@janeirodigital/sai-css": "^1.0.0-rc.24"
  }
}
