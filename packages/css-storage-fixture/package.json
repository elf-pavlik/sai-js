{
  "name": "css-storage-fixture",
  "private": true,
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "community-solid-server -c config.json -l debug -f ./dev/pod",
    "auth": "community-solid-server -c auth.json -p 4800 -l debug -f ./dev/auth",
    "registry": "community-solid-server -c registry.json -p 4600 -l debug -f ./dev/registry -s http://sparql/sparql",
    "data": "community-solid-server -c data.json -p 4700 -l debug -f ./dev/data",
    "clean": "npm run clean:empty-directories && npm run clean:notification-channels",
    "clean:empty-directories": "find . -type d -empty -delete",
    "clean:notification-channels": "rm -rf ./dev/pod/.internal/notifications/*"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@solid/community-server": "8.0.0-alpha.2",
    "@elfpavlik/sai-components": "1.0.0-rc.29"
  }
}
